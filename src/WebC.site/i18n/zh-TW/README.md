# 承載元件庫文件與示範站點的路由入口

- **全螢幕佈局**：絕對定位撐滿視口，內建滾動與導航。
- **動態路由**：解析 URL 路徑，動態載入元件示範或首頁。
- **多語言**：即時回應並更新導覽列與頁面文案。
- **狀態切換**：自動維護載入中與首頁的樣式類別。

## 使用演示

```html
<c-webc-index></c-webc-index>
```

## 樣式類

元件會在自身切換以下類別名稱：

- `Ing`：路由載入或語言包解析中。
- `A`：目前處於首頁。