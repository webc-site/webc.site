# 自動切換導航選單與返回按鈕

- 自動包裹子元素生成導航菜單和返回按鈕結構
- 自動追蹤路由歷史並計算上一級返回路徑
- 路由匹配菜單項時顯示導航菜單，否則顯示返回按鈕

## 使用演示

```html
<c-nav-l>
  <a href="/">首頁</a>
  <a href="/doc">文件</a>
</c-nav-l>
```

## 樣式類

- `B`：啟動返回按鈕狀態。顯示返回按鈕並隱藏導航選單。由組件內部根據路由自動切換。