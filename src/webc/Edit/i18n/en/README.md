# Edit Button

- Contains right angle edit icon button
- Mouse-over prompt for editing, multi-language support

## Use the demo

```html
<c-edit href="https://github.com"></c-edit>
```

## Interface parameters

The component label is `<c-edit>`, and the jump address can be edited through the `href` attribute configuration.