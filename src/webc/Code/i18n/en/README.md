# Code Button

- Contain code icon button
- Mouse hover prompt code, supports multiple languages

## Use the demo

```html
<c-code href="https://github.com"></c-code>
```

## Interface parameters

The component label is `<c-code>`, and the code jump address can be configured through the `href` attribute.