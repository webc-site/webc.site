# 承载组件库文档与演示站点的路由入口

- **全屏布局**：绝对定位撑满视口，内置滚动与导航。
- **动态路由**：解析 URL 路径，动态加载组件演示或首页。
- **多语言**：实时响应并更新导航栏与页面文案。
- **状态切换**：自动维护加载中与首页的样式类。

## 使用演示

```html
<c-webc-site></c-webc-site>
```

## 样式类

组件会在自身切换以下类名：

- `Ing`：路由加载或语言包解析中。
- `A`：当前处于首页。