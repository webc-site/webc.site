# 自动切换导航菜单与返回按钮

- 自动包裹子元素生成导航菜单和返回按钮结构
- 自动追踪路由历史并计算上一级返回路径
- 路由匹配菜单项时显示导航菜单，否则显示返回按钮

## 使用演示

```html
<c-nav-l>
  <a href="/">首页</a>
  <a href="/doc">文档</a>
</c-nav-l>
```

## 样式类

- `B`：激活返回按钮状态。显示返回按钮并隐藏导航菜单。由组件内部根据路由自动切换。