# md/safeHtm.js

Markdown text sa ligtas na HTML

## default(val)

I-parse at i-convert ang Markdown text sa XSS-protected, secure na HTML strings

parameter:
- `val` : Markdown text na iko-convert

Return value: ligtas na HTML string