# On.js

Giám sát sự kiện ràng buộc hàng loạt

## On(elem, dict)

Liên kết các trình xử lý sự kiện phần tử DOM theo lô

tham số:
- `elem` : Phần tử DOM
- `dict` : Ánh xạ các sự kiện và lệnh gọi lại

Giá trị trả về: Xóa tất cả các hàm nghe sự kiện bị ràng buộc