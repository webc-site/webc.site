# md/safeHtm.js

Đánh dấu văn bản thành HTML an toàn

## default(val)

Phân tích cú pháp và chuyển đổi văn bản Markdown thành các chuỗi HTML an toàn, được bảo vệ bằng XSS

tham số:
- `val` : Văn bản đánh dấu cần được chuyển đổi

Giá trị trả về: chuỗi HTML an toàn