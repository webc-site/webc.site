# i18n/store.js

Quản lý lưu trữ cục bộ đa ngôn ngữ

## get()

Nhận ID ngôn ngữ mặc định được tính toán trong bộ nhớ đệm cục bộ hoặc được tính toán bằng cách khớp ngôn ngữ trình duyệt

Giá trị trả về: ID ngôn ngữ

## set(id)

Lưu trữ ID ngôn ngữ hiện tại cục bộ

tham số:
- `id` : ID ngôn ngữ