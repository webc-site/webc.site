# dom/header.js

Tạo cấu trúc DOM thanh tiêu đề

## default()

Tạo và kết hợp cấu trúc DOM tiêu đề của thẻ tiêu đề, h1 và b

Giá trị trả về:

- `header` : Phần tử chứa của thanh tiêu đề
- `h1` : thành phần tiêu đề
- `b` : phụ đề hoặc thành phần thông tin bổ sung