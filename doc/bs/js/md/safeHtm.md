# md/safeHtm.js

Označite tekst u siguran HTML

## default(val)

Parsirajte i pretvorite Markdown tekst u XSS zaštićene, sigurne HTML nizove

parametar:
- `val` : Markdown tekst koji treba pretvoriti

Povratna vrijednost: siguran HTML niz