# md/htm.js

Markdown de texto para string HTML

## default(text)

Analise e converta texto formatado em Markdown em strings HTML

parâmetro:
- `text` : Texto Markdown a ser convertido

Valor de retorno: string HTML convertida