# md/safeHtm.js

Markdown de texto para HTML seguro

## default(val)

Analise e converta texto Markdown em strings HTML seguras e protegidas por XSS

parâmetro:
- `val` : Texto Markdown a ser convertido

Valor de retorno: string HTML segura