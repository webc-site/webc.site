# icoTip.js

تحديد وتسجيل مكون الرمز مع تلميحات التدويل

## default(i18n_url, tag, key)

قم بتسجيل مكون عنصر HTML مخصص باستخدام فقاعات مطالبة التدويل

المعلمة:
- `i18n_url` : عنوان الطلب لتكوين التدويل
- `tag` : تخصيص اسم العلامة لعنصر HTML
- `key` : القيمة الأساسية لنص المطالبة في قاموس التدويل