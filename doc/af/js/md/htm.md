# md/htm.js

Markdown teks na HTML-string

## default(text)

Ontleed en omskep Markdown-geformateerde teks in HTML-stringe

parameter:
- `text` : Markdown-teks wat omgeskakel moet word

Terugkeerwaarde: omgeskakelde HTML-string