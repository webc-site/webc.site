# md/htm.js

ტექსტის მარკირება HTML სტრიქონზე

## default(text)

Markdown-ის ფორმატირებული ტექსტის გაანალიზება და კონვერტირება HTML სტრიქონებში

პარამეტრი:
- `text` : მარკდაუნის ტექსტი, რომელიც უნდა გარდაიქმნას

დაბრუნების მნიშვნელობა: კონვერტირებული HTML სტრიქონი