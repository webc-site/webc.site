# md/htm.js

ทำเครื่องหมายข้อความเป็นสตริง HTML

## default(text)

แยกวิเคราะห์และแปลงข้อความที่จัดรูปแบบ Markdown เป็นสตริง HTML

พารามิเตอร์:
- `text` : ข้อความมาร์กดาวน์ที่ต้องการแปลง

ค่าที่ส่งคืน: สตริง HTML ที่แปลงแล้ว