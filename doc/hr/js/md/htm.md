# md/htm.js

Markdown tekst u HTML niz

## default(text)

Raščlanite i pretvorite Markdown formatirani tekst u HTML nizove

parametar:
- `text` : Markdown tekst koji treba pretvoriti

Povratna vrijednost: konvertirani HTML niz