# c-t.js

HTML beynəlmiləlləşdirilməsi

`<c-t>key</c-t>` teqi fərdiləşdirilmişdir və `key` dil paketinin açarıdır.

## cTranSet(el, genUrl)

`c-t` üçün mətni təyin edin.

parametr:
- `el` : kök element, qlobal `document`-də ötürülə bilər
- `genUrl` : dil paketi lüğətini əldə etmək funksiyası
  - parametr
    - `code` : dil kodu
  - Qaytarma dəyəri: dil paketi lüğəti obyekti və ya bu obyekti həll edən Promise