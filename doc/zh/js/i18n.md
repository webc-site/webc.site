# i18n.js

多语言状态管理与变更监听

## LANG_LI

可用语言配置列表

## lang()

获取当前语言 ID

返回值: 当前语言 ID

## langCode()

获取当前语言代号

返回值: 当前语言代号，如 `"zh"`

## onLang(func)

绑定语言变更的监听回调

参数:
- `func` : 语言变更时的回调函数
  - 参数
    - `code` : 语言代号
    - `id` : 语言 ID

返回值: 取消绑定的清理函数

## langSet(id)

切换并广播当前语言

参数:
- `id` : 目标语言 ID
