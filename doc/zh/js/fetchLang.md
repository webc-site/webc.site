# fetchLang.js

异步加载语言包并绑定更新回调

## fetchLang(getDict)

创建语言包监听器，用于在语言改变时异步获取语言包并执行刷新回调

参数:
- `getDict` : 获取语言包的异步函数
  - 参数
    - `code` : 语言代码
  - 返回值 : 语言包字典

返回值: 注册刷新回调的函数
- 参数
  - `refresh` : 语言更新时的刷新回调函数
    - 参数
      - `i18n` : 语言包字典对象
      - `code` : 当前语言代码
- 返回值 : 注销该刷新回调的函数
