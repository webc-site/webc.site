# parentScroll.js

Pobierz element kontenera przewijania przodka

## default(el)

Przejdź w górę od określonego elementu, znajdź i zwróć zawierający go element kontenera przewijania (taki jak wbudowany kontener przewijania ShadowRoot `C-VS`)

parametr:
- `el` : początkowy element DOM

Wartość zwracana: przewiń element DOM kontenera