# dom/header.js

Utwórz strukturę DOM paska nagłówka

## default()

Utwórz i połącz strukturę DOM nagłówka z nagłówkiem, znacznikami h1 i b

Wartość zwracana:

- `header` : Element kontenera paska nagłówka
- `h1` : element tytułu
- `b` : podtytuł lub dodatkowy element informacyjny