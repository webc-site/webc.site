# c-t.js

HTMLni xalqarolashtirish

`<c-t>key</c-t>` tegi moslashtirilgan va `key` til paketining kalitidir.

## cTranSet(el, genUrl)

`c-t` uchun matnni o'rnating.

parametr:
- `el` : ildiz element, global `document` da uzatilishi mumkin
- `genUrl` : til toʻplami lugʻatini olish funksiyasi
  - parametr
    - `code` : til kodi
  - Qaytish qiymati: til to'plami lug'at ob'ekti yoki ushbu ob'ektni hal qiladigan va'da