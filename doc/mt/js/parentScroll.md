# parentScroll.js

Ikseb l-element tal-kontenitur tal-iscroll tal-antenat

## default(el)

Aqsa 'l fuq mill-element speċifikat, sib u rritorna l-element tal-iscroll container li jkun fih (bħal `C-VS`'s ShadowRoot built-in scroll container)

parametru:
- `el` : element DOM tal-bidu

Valur tar-ritorn: iscroll container element DOM