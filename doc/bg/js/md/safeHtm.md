# md/safeHtm.js

Маркиране на текст в безопасен HTML

## default(val)

Анализирайте и преобразувайте Markdown текст в XSS защитени, сигурни HTML низове

параметър:
- `val` : Markdown текст за преобразуване

Върната стойност: безопасен HTML низ