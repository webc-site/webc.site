# md/htm.js

Markdown teks menyang string HTML

## default(text)

Parse lan ngowahi teks format Markdown menyang strings HTML

parameter:
- `text` : Markdown teks sing arep diowahi

Nilai bali: string HTML sing diowahi