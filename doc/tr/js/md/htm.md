# md/htm.js

Metni HTML dizesine işaretleme

## default(text)

Markdown formatlı metni ayrıştırın ve HTML dizelerine dönüştürün

parametre:
- `text` : Dönüştürülecek işaretleme metni

Dönüş değeri: dönüştürülmüş HTML dizesi