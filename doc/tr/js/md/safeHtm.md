# md/safeHtm.js

Metni güvenli HTML'ye işaretleme

## default(val)

Markdown metnini ayrıştırın ve XSS korumalı, güvenli HTML dizelerine dönüştürün

parametre:
- `val` : Dönüştürülecek işaretleme metni

Dönüş değeri: güvenli HTML dizesi