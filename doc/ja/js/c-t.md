# c-t.js

HTML の国際化

タグ `<c-t>key</c-t>` はカスタマイズされており、`key` は言語パックのキーです。

## cTranSet(el, genUrl)

`c-t`のテキストを設定します。

パラメータ:
- `el` : ルート要素、グローバルを `document` に渡すことができます
- `genUrl` : 言語パック辞書を取得する関数
  - パラメータ
    - `code` : 言語コード
  - 戻り値: 言語パック辞書オブジェクト、またはこのオブジェクトに解決される Promise