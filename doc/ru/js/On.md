# On.js

Мониторинг событий пакетной привязки

## On(elem, dict)

Пакетное связывание прослушивателей событий элемента DOM

параметр:
- `elem` : элемент DOM.
- `dict` : Сопоставление событий и обратных вызовов.

Возвращаемое значение: удалить все связанные функции прослушивания событий.