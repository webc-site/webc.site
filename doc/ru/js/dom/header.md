# dom/header.js

Создайте структуру DOM панели заголовка.

## default()

Создайте и объедините структуру DOM заголовка из тегов h1 и b.

Возвращаемое значение:

- `header` : элемент-контейнер панели заголовка.
- `h1` : элемент заголовка.
- `b`: подзаголовок или элемент дополнительной информации.