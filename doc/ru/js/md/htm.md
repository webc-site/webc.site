# md/htm.js

Текст разметки в строку HTML

## default(text)

Разбор и преобразование текста в формате Markdown в строки HTML.

параметр:
- `text`: текст уценки, который нужно преобразовать.

Возвращаемое значение: преобразованная строка HTML.