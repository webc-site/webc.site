# md/htm.js

Texte Markdown en chaîne HTML

## default(text)

Analyser et convertir le texte au format Markdown en chaînes HTML

paramètre:
- `text` : Texte Markdown à convertir

Valeur de retour : chaîne HTML convertie