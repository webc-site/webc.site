# dom/header.js

צור את מבנה ה-DOM של שורת הכותרת

## default()

צור ושלב את מבנה ה-DOM של הכותרת של תגיות כותרת, h1 ו-b

ערך החזרה:

- `header` : רכיב מכיל של סרגל הכותרת
- `h1` : רכיב כותרת
- `b` : כתובית או רכיב מידע נוסף