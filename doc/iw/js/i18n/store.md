# i18n/store.js

ניהול אחסון מקומי רב לשוני

## get()

קבל את מזהה שפת ברירת המחדל המחושב במטמון מקומי או מחושב לפי התאמת שפת הדפדפן

ערך החזרה: מזהה שפה

## set(id)

שמור את מזהה השפה הנוכחית באופן מקומי

פָּרָמֶטֶר:
- `id` : מזהה שפה