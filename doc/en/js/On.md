# On.js

Batch binding event monitoring

## On(elem, dict)

Bind DOM element event listeners in batches

parameter:
- `elem` : DOM element
- `dict` : Mapping of events and callbacks

Return value: Remove all bound event listener functions