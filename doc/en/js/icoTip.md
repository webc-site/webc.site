# icoTip.js

Define and register an icon component with internationalization hints

## default(i18n_url, tag, key)

Register a custom HTML element component with internationalization prompt bubbles

parameter:
- `i18n_url` : Request address for internationalization configuration
- `tag` : Customize the tag name of the HTML element
- `key` : The key value of the prompt text in the internationalization dictionary