# parentScroll.js

Get the ancestor scroll container element

## default(el)

Traverse upward from the specified element, find and return the scroll container element containing it (such as `C-VS`'s ShadowRoot built-in scroll container)

parameter:
- `el` : starting DOM element

Return value: scroll container DOM element