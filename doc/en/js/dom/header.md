# dom/header.js

Create the header bar DOM structure

## default()

Create and combine the header DOM structure of header, h1 and b tags

Return value:

- `header` : Container element of the header bar
- `h1` : title element
- `b` : subtitle or additional information element