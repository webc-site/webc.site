# md/safeHtm.js

Markdown text to safe HTML

## default(val)

Parse and convert Markdown text into XSS-protected, secure HTML strings

parameter:
- `val` : Markdown text to be converted

Return value: safe HTML string