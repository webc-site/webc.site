# parentScroll.js

Kritt d'Virgänger Scroll Container Element

## default(el)

Traverséiert no uewen vum spezifizéierte Element, fann a gitt de Scroll Container Element mat deem et enthält (wéi `C-VS`ShadowRoot agebaute Scroll Container)

parameter:
- `el` : Start DOM Element

Retour Wäert: scroll Container DOM Element