표준 `css`을 사용하고, `-webkit-`로 시작하는 `css`를 쓰지 마세요(프레임워크가 자동으로 삽입합니다).

`css nesting` , `flex` 등과 같은 최신 `css` 기능을 사용하여 구성 요소 스타일을 개발합니다.

동일한 값에 대해 여러 CSS 선택기를 한곳에서 작성, 정의 및 재사용할 수 있으며 `stylus` 기능이 비활성화됩니다.

픽셀 단위는 `px`이며, 값은 정수여야 합니다.

Apple의 최신 Liquid Glass 디자인 미학을 따르세요.

`svg` 이미지를 `svg` 폴더에 넣고 `styl`에서 상대 경로로 사용하세요.

외부 그림자를 사용하지 말고, 내부 그림자를 더 자주 사용하고, 내부 그림자를 대체해서는 안 됩니다.

`font-family`을 설정하지 마세요.

`stylus` 고급 기능 없이 가장 기본적인 변수와 들여쓰기 구문 형식만 사용합니다.