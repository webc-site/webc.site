`src/js` Allgemeng benotzt js Scripte si wéi follegt

- `import { D, newEl, newLi } from "../js/dom.js"`
  - `D` ass d'Ofkierzung vun `document`
  - `newEl` erstellt en `dom` Element. Wann Dir méi Elementer wëllt erstellen, benotzt `newLi` fir se a Chargen ze kreéieren
- `import { cE } from "../js/cE.js"` Erstellt e personaliséierten Element
- `import { On } from "../js/On.js"` Benotzt fir Eventverbindung an Unbindung ëmzesetzen