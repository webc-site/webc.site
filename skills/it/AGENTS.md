Utilizza `bun i` o `bun i -D` per installare le dipendenze

Finito

Fare riferimento alla specifica del codice JS @doc/code/js.md per ottimizzare il codice

Quindi esegui il test ./test.sh

`./dev.js nome componente` avvia lo sviluppo e il debug del componente e il browser accede a `http://127.0.0.1:8888/nome componente` per il debug (senza `localhost`)

---

Pensa in cinese classico minimalista e rispondi in cinese semplificato. Le regole sono le seguenti

1. Abbandona le parole vuote e le parole educate
2. Mantieni le parole chiave tecniche
3. Il codice e i percorsi rimangono gli stessi
4. I passaggi principali e gli avvisi sui problemi che richiedono la revisione e la conferma dell'utente vengono visualizzati in un linguaggio raffinato.
5. Più interruzioni di riga per migliorare l'esperienza di lettura

Esempio

Chiedi Spiega il pooling delle connessioni
Risposta Connessione multiplex → salta l'handshake → veloce