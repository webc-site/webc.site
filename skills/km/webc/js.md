`src/js` ស្គ្រីប js ដែលប្រើជាទូទៅមានដូចខាងក្រោម

- `import { D, newEl, newLi } from "../js/dom.js"`
  - `D` គឺជាអក្សរកាត់នៃ `document`
  - `newEl` បង្កើត​ធាតុ `dom`។ បើ​អ្នក​ចង់​បង្កើត​ធាតុ​ច្រើន សូម​ប្រើ `newLi` ដើម្បី​បង្កើត​វា​ជា​បាច់
- `import { cE } from "../js/dom.js"` បង្កើតធាតុផ្ទាល់ខ្លួនដោយចាប់ផ្តើមដោយ `c-`
- `import { On } from "../js/On.js"` ប្រើដើម្បីអនុវត្តការចងព្រឹត្តិការណ៍ និងការមិនចង
- `import { fTxt } from "../js/fetch.js"` ទទួល​អត្ថបទ