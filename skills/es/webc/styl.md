Utilice el estándar `css`, no escriba `css` comenzando con `-webkit-` (el marco lo inyectará automáticamente)

Desarrolle estilos de componentes con funciones `css` modernas como `css nesting`, `flex`, etc.

Para el mismo valor, se pueden escribir, definir y reutilizar varios selectores CSS en un solo lugar, y la función de `stylus` está deshabilitada.

La unidad de píxeles es `px` y el valor debe ser un número entero.

Sigue la estética del diseño del último Liquid Glass de Apple

`svg` Coloque la imagen en la carpeta `svg` y úsela en `styl` con una ruta relativa

No utilice sombras exteriores, utilice sombras interiores con más frecuencia y las sombras interiores no deben desplazarse.

No establezca `font-family`

`stylus` Solo utiliza las variables más básicas y el formato de sintaxis de sangría, sin funciones avanzadas