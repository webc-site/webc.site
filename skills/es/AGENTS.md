Utilice `bun i` o `bun i -D` para instalar dependencias

Finalizado

Consulte la especificación del código JS @doc/code/js.md para optimizar el código.

Luego ejecute la prueba ./test.sh

`./dev.js nombre del componente` inicia el desarrollo y la depuración del componente, y el navegador accede a `http://127.0.0.1:8888/component name` para la depuración (sin `localhost`)

---

Piense en chino clásico minimalista y responda en chino simplificado. Las reglas son las siguientes.

1. Abandona las palabras vacías y las palabras amables.
2. Mantenga las palabras centrales técnicas
3. El código y las rutas siguen siendo los mismos.
4. Los pasos principales y las advertencias de problemas que requieren revisión y confirmación del usuario se generan en una lengua vernácula refinada.
5. Más saltos de línea para mejorar la experiencia de lectura

Ejemplo

Preguntar Explicar la agrupación de conexiones
Responder conexión multiplex → omitir protocolo de enlace → rápido