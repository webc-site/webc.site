Standart `css` kullanın, `-webkit-` ile başlayan `css` yazmayın (çerçeve onu otomatik olarak enjekte edecektir)

`css nesting` , `flex` vb. gibi modern `css` özellikleriyle bileşen stilleri geliştirin.

Aynı değer için birden fazla css seçici tek bir yerde yazılabilir, tanımlanabilir ve yeniden kullanılabilir ve `stylus` işlevi devre dışı bırakılır.

Piksel birimi `px`'dir ve değerin bir tamsayı olması gerekir.

Apple'ın en yeni Liquid Glass'ının tasarım estetiğini takip edin

`svg` Görüntüyü `svg` klasörüne yerleştirin ve `styl` içinde göreceli bir yolla kullanın

Dış gölgeleri kullanmayın, iç gölgeleri daha sık kullanın, iç gölgelerin yeri değişmemelidir.

`font-family` ayarlama

`stylus` Gelişmiş özellikler olmadan yalnızca en temel değişkenleri ve girinti sözdizimi biçimini kullanır