Use `bun i` or `bun i -D` to install dependencies

Finished

Refer to JS code specification @doc/code/js.md to optimize the code

Then run the ./test.sh test

`./dev.js component name` starts the development and debugging of the component, and the browser accesses `http://127.0.0.1:8888/component name` for debugging (without `localhost`)

---

Think in minimalist classical Chinese and reply in simplified Chinese. The rules are as follows

1. Abandon empty words and polite words
2. Keep technical core words
3. Code and paths remain the same
4. Core steps and problem warnings that require user review and confirmation are output in refined vernacular.
5. More line breaks to improve reading experience

Example

Ask Explain connection pooling
Answer Multiplex connection → skip handshake → fast