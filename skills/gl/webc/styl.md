Use o estándar `css`, non escriba `css` que comece por `-webkit-` (o marco inxectaao automaticamente)

Desenvolve estilos de compoñentes con funcións modernas de `css` como `css nesting` , `flex` etc.

Para o mesmo valor, pódense escribir, definir e reutilizar varios selectores de CSS nun só lugar, e a función de `stylus` está desactivada.

A unidade de píxeles é `px` e o valor debe ser un número enteiro.

Sigue a estética do deseño do último Liquid Glass de Apple

`svg` Coloque a imaxe no cartafol `svg` e utilízaa en `styl` cun camiño relativo

Non use sombras exteriores, use sombras interiores con máis frecuencia e as sombras interiores non deben desprazarse.

Non configure `font-family`

`stylus` Só utiliza as variables máis básicas e o formato de sintaxe de sangría, sen funcións avanzadas