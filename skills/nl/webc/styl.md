Gebruik standaard `css`, schrijf niet `css` beginnend met `-webkit-` (het framework zal het automatisch injecteren)

Ontwikkel componentstijlen met moderne `css` functies zoals `css nesting` , `flex` etc.

Voor dezelfde waarde kunnen meerdere css-selectors op één plaats worden geschreven, gedefinieerd en hergebruikt, en de functie van `stylus` is uitgeschakeld.

De pixeleenheid is `px` en de waarde moet een geheel getal zijn.

Volg de ontwerpesthetiek van Apple's nieuwste Liquid Glass

`svg` Plaats de afbeelding in de map `svg` en gebruik deze in `styl` met een relatief pad

Gebruik geen buitenste schaduwen, gebruik vaker innerlijke schaduwen en de binnenste schaduwen mogen niet worden verplaatst.

Stel `font-family` niet in

`stylus` Gebruikt alleen de meest elementaire variabelen en de syntaxisindeling voor inspringingen, zonder geavanceerde functies