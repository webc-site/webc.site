使用 `bun i` 或 `bun i -D` 安装依赖

写完

参考 JS 代码规范 @doc/code/js.md 优化代码

然后运行 ./test.sh 测试

`./dev.js 组件名` 启动组件的开发调试，浏览器访问 `http://127.0.0.1:8888/组件名` 调试（不用 `localhost`）

---

用极简文言进行思考，用简体中文回复，规则如下

1. 弃虚词、客套话
2. 保留技术核心词
3. 代码、路径保持原样
4. 需用户审查确认的核心步骤、问题警告，用精炼的白话输出
5. 多换行，提高阅读体验

示例

问 解释连接池
答 复用连接 → 跳过握手 → 速度快