用标准 `css`，不写 `-webkit-` 开头的 `css`（框架会自动注入）

用现代 `css` 特性（如 `css nesting` 、`flex` 等） 开发组件样式

相同的值，可以写多个 css 选择器，一处定义复用，禁用 `stylus` 的函数

像素单位用 `px`，数值必须是整数

遵循苹果最新液态玻璃的设计美学

`svg` 图片放到 `svg` 文件夹下，用相对路径在 `styl` 中使用

不用外阴影，多用内阴影，内阴影不要有位移

不设置 `font-family`

`stylus` 只用最基本的变量、缩进语法格式，不用高阶特性