Notaðu staðlaða `css`, ekki skrifa `css` byrja á `-webkit-` (ramminn mun sjálfkrafa dæla því inn)

Þróaðu íhlutastíla með nútímalegum `css` eiginleikum eins og `css nesting` , `flex` o.s.frv.

Fyrir sama gildi er hægt að skrifa, skilgreina og endurnýta marga CSS-veljara á einum stað og virkni `stylus` er óvirk.

Dílaeiningin er `px` og gildið verður að vera heil tala.

Fylgdu hönnunarfagurfræði nýjustu Liquid Glass frá Apple

`svg` Settu myndina í `svg` möppuna og notaðu hana í `styl` með hlutfallslegri slóð

Ekki nota ytri skugga, notaðu innri skugga oftar og innri skugga ætti ekki að færa til.

Ekki stilla `font-family`

`stylus` Notar aðeins grunnbreytur og inndráttarsetningafræði, án háþróaðra eiginleika

Það er bannað að nota `@import` til að flytja inn aðra íhlutastíl (þar á meðal hluti `_.styl` og `demo/_.styl`); íhlutaháðir stílar eru fluttir inn í gegnum JS skrá íhlutarins