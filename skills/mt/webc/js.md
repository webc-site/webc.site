`src/js` L-iskripts js użati b'mod komuni huma kif ġej

- `import { D, newEl, newLi } from "../js/dom.js"`
  - `D` hija l-abbrevjazzjoni ta' `document`
  - `newEl` joħloq element `dom`. Jekk trid toħloq elementi multipli, uża `newLi` biex toħloqhom f'lottijiet
- `import { cE } from "../js/dom.js"` Oħloq element personalizzat
- `import { On } from "../js/On.js"` Użat biex jimplimenta l-irbit u l-unbinding tal-avveniment