Uża l-istandard `css`, tiktebx `css` li tibda b'`-webkit-` (il-qafas jinjettaha awtomatikament)

Żviluppa stili ta' komponenti b'karatteristiċi moderni `css` bħal `css nesting` , `flex` eċċ.

Għall-istess valur, seletturi css multipli jistgħu jinkitbu, jiġu definiti u jerġgħu jintużaw f'post wieħed, u l-funzjoni ta '`stylus` hija diżattivata.

L-unità tal-pixel hija `px`, u l-valur għandu jkun numru sħiħ.

Segwi l-estetika tad-disinn ta 'l-aħħar Liquid Glass ta' Apple

`svg` Poġġi l-immaġni fil-fowlder `svg` u użaha f'`styl` b'passaġġ relattiv

Tużax dellijiet ta 'barra, uża dellijiet ta' ġewwa aktar spiss, u d-dellijiet ta 'ġewwa m'għandhomx jiġu spostati.

Issettjax `font-family`

`stylus` Juża biss il-varjabbli l-aktar bażiċi u l-format tas-sintassi tal-indentazzjoni, mingħajr karatteristiċi avvanzati

Huwa pprojbit li tuża `@import` biex timporta stili ta' komponenti oħra (inklużi komponenti `_.styl` u `demo/_.styl`); stili dipendenti fuq il-komponenti huma importati permezz tal-fajl JS tal-komponent