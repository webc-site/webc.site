השתמש ב-`bun i` או `bun i -D` כדי להתקין תלות

גָמוּר

עיין במפרט קוד JS @doc/code/js.md כדי לבצע אופטימיזציה של הקוד

לאחר מכן הפעל את מבחן ./test.sh

`./dev.js שם רכיב` מתחיל את הפיתוח ואת ניפוי הבאגים של הרכיב, והדפדפן ניגש ל`http://127.0.0.1:8888/component name` לצורך ניפוי באגים (ללא `localhost`)

---

תחשוב בסינית קלאסית מינימליסטית והשב בסינית פשוטה. הכללים הם כדלקמן

1. עזוב מילים ריקות ומילות נימוס
2. שמור על מילות ליבה טכניות
3. הקוד והנתיבים נשארים זהים
4. שלבי ליבה ואזהרות בעיות הדורשות סקירה ואישור של משתמשים מופקים בשפת לשון מעודנת.
5. מעברי שורות נוספים לשיפור חווית הקריאה

דוּגמָה

שאל הסבר איחוד חיבורים
תשובה חיבור מרובה → דלג על לחיצת יד → מהיר