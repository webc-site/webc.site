使用 `bun i` 或 `bun i -D` 安裝依賴

寫完

參考 JS 程式碼規格 @doc/code/js.md 最佳化程式碼

然後執行 ./test.sh 測試

`./dev.js 元件名稱` 啟動元件的開發除錯，瀏覽器存取 `http://127.0.0.1:8888/元件名稱` 偵錯（不用 `localhost`）

---

用極簡文言思考，用簡體中文回复，規則如下

1. 棄虛詞、客套話
2. 保留技術核心詞
3. 程式碼、路徑保持原樣
4. 需使用者審查確認的核心步驟、問題警告，用精煉的白話輸出
5. 多換行，提升閱讀體驗

範例

問 解釋連接池
答 復用連接 → 跳過握手 → 速度快