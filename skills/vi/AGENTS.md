Sử dụng `bun i` hoặc `bun i -D` để cài đặt các phần phụ thuộc

Hoàn thành

Tham khảo đặc tả mã JS @doc/code/js.md để tối ưu hóa mã

Sau đó chạy thử nghiệm ./test.sh

`./dev.js tên thành phần` bắt đầu phát triển và gỡ lỗi thành phần, đồng thời trình duyệt truy cập `http://127.0.0.1:8888/tên thành phần` để gỡ lỗi (không có `localhost`)

---

Hãy suy nghĩ bằng tiếng Trung cổ điển tối giản và trả lời bằng tiếng Trung giản thể. Các quy tắc như sau

1. Bỏ đi những lời nói trống rỗng và những lời lịch sự
2. Giữ các từ cốt lõi về mặt kỹ thuật
3. Mã và đường dẫn vẫn giữ nguyên
4. Các bước cốt lõi và cảnh báo sự cố yêu cầu người dùng xem xét và xác nhận sẽ được xuất ra bằng tiếng bản địa được tinh chỉnh.
5. Thêm ngắt dòng để cải thiện trải nghiệm đọc

Ví dụ

Hỏi Giải thích tổng hợp kết nối
Trả lời Kết nối Multiplex → bỏ qua bắt tay → nhanh