Standart `css` dan foydalaning, `-webkit-` dan boshlab `css` yozmang (ramka uni avtomatik ravishda kiritadi)

Komponent uslublarini zamonaviy `css` xususiyatlari, masalan, `css nesting` , `flex` va hokazolarni ishlab chiqing.

Xuddi shu qiymat uchun bir nechta CSS selektorlari bir joyda yozilishi, aniqlanishi va qayta ishlatilishi mumkin va `stylus` funksiyasi o'chiriladi.

Piksel birligi `px` va qiymat butun son bo'lishi kerak.

Apple kompaniyasining so'nggi suyuq shishasining dizayn estetikasiga rioya qiling

`svg` Rasmni `svg` jildiga joylashtiring va uni nisbiy yo'l bilan `styl` da ishlating

Tashqi soyalarni ishlatmang, ichki soyalarni tez-tez ishlating va ichki soyalarni almashtirmaslik kerak.

`font-family` belgilanmasin

`stylus` Kengaytirilgan funksiyalarsiz faqat eng asosiy oʻzgaruvchilar va chekinish sintaksisi formatidan foydalanadi