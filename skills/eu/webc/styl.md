Erabili `css` estandarra, ez idatzi `css` `-webkit-`-tik hasita (esparruak automatikoki sartuko du)

Garatu osagai-estiloak `css` ezaugarri modernoekin, hala nola, `css nesting` , `flex` etab.

Balio berdinerako, css hautatzaile anitz idatzi, definitu eta berrerabili daitezke leku batean, eta `stylus` funtzioa desgaituta dago.

Pixel-unitatea `px` da, eta balioak zenbaki oso bat izan behar du.

Jarraitu Apple-ren azken Liquid Glass-aren diseinu-estetika

`svg` Jarri irudia `svg` karpetan eta erabili `styl`n bide erlatibo batekin

Ez erabili kanpoko itzalak, erabili barruko itzalak maizago, eta barruko itzalak ez dira desplazatu behar.

Ez ezarri `font-family`

`stylus` Aldagai oinarrizkoenak eta koska-sintaxi formatua soilik erabiltzen ditu, eginbide aurreratu gabe