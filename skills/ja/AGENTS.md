`bun i` または `bun i -D` を使用して依存関係をインストールします

終了した

コードを最適化するには、JS コード仕様 @doc/code/js.md を参照してください。

次に、./test.sh テストを実行します。

`./dev.js コンポーネント名` によりコンポーネントの開発とデバッグが開始され、ブラウザはデバッグのために `http://127.0.0.1:8888/コンポーネント名` にアクセスします (`localhost` なし)

---

最小限の古典中国語で考え、簡体字中国語で応答します。ルールは次のとおりです

1. 空虚な言葉や丁寧な言葉を捨てる
2. 技術的な核心語を保持する
3. コードとパスは同じままです
4. ユーザーのレビューと確認が必要な主要な手順と問題の警告は、洗練された言語で出力されます。
5. 改行を増やすと読みやすさが向上します

例

接続プーリングの説明を求める
応答 多重接続 → ハンドシェイクスキップ → 高速