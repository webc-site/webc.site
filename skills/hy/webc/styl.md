Օգտագործեք ստանդարտ `css`, մի գրեք `css` սկսած `-webkit-`-ով (շրջանակն այն ավտոմատ կերպով կներարկի)

Մշակեք բաղադրիչների ոճեր ժամանակակից `css` հատկանիշներով, ինչպիսիք են `css nesting` , `flex` և այլն:

Նույն արժեքի համար մի քանի css ընտրիչներ կարող են գրվել, սահմանվել և նորից օգտագործվել մեկ տեղում, և `stylus` ֆունկցիան անջատված է:

Պիքսելային միավորը `px` է, և արժեքը պետք է լինի ամբողջ թիվ:

Հետևեք Apple-ի վերջին հեղուկ ապակիների դիզայնի էսթետիկային

`svg` Տեղադրեք պատկերը `svg` թղթապանակում և օգտագործեք այն `styl`-ում՝ հարաբերական ճանապարհով

Մի օգտագործեք արտաքին ստվերներ, ավելի հաճախ օգտագործեք ներքին ստվերները, իսկ ներքին ստվերները չպետք է տեղաշարժվեն:

Մի սահմանեք `font-family`

`stylus` Օգտագործում է միայն ամենահիմնական փոփոխականները և ներքևի շարահյուսական ձևաչափը՝ առանց առաջադեմ գործառույթների